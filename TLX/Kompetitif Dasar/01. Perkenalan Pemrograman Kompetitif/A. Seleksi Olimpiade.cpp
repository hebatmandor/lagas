#include <bits/stdc++.h>
using namespace std;

void solve(int n, int m){
    string cek, id[80];
    int arrA[80], arrB[80], arrC[80];
    cin >> cek;
    for (int i = 0; i < n; i++)
    {
        cin >> id[i] >> arrA[i] >> arrB[i] >> arrC[i];
    }
    
}

int main(){
    int T, N, M;
    cin >> T;
    for (int i = 0; i < T; i++)
    {
        cin >> N >> M;
        solve(N, M);
    }
}